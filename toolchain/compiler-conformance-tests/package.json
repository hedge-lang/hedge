{
  "name": "@hedge-lang/compiler-conformance-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "pretest": "npm run build --workspace=@hedge-lang/compiler",
    "precheck": "npm run build --workspace=@hedge-lang/compiler",
    "test": "vitest run",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@hedge-lang/compiler": "*"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
