trait Add {
  fn add(self, rhs: Self) -> Self;
}

struct Point {
  x: i32,
}

impl Add for Point {
  fn add(self, rhs: Self) -> Self { self }
}

fn main() {
  let mut p = Point { x: 1 };
  let q = Point { x: 2 };
  p += q;
  print(p.x);
}
